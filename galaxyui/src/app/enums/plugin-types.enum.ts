export enum PluginNames {
    action = 'Action',
    cache = 'Cache',
    callback = 'Callback',
    cliconf = 'CLI Config',
    connection = 'Connection',
    filter = 'Filter',
    inventory = 'Inventory',
    lookup = 'Lookup',
    netconf = 'Network Config',
    shell = 'Shell',
    strategy = 'Strategy',
    terminal = 'Terminal',
}

export enum PluginTypes {
    action = 'Action Plugin',
    cache = 'Cache Plugin',
    callback = 'Callback Plugin',
    cliconf = 'CLI Config Plugin',
    connection = 'Connection Plugin',
    filter = 'Filter Plugin',
    inventory = 'Inventory Plugin',
    lookup = 'Lookup Plugin',
    netconf = 'Network Config Plugin',
    shell = 'Shell Plugin',
    strategy = 'Strategy Plugin',
    terminal = 'Terminal Plugin',
}
