export enum PluginNames {
    action_plugin = 'Action',
    cache_plugin = 'Cache',
    callback_plugin = 'Callback',
    cliconf_plugin = 'CLI Config',
    connection_plugin = 'Connection',
    filter_plugin = 'Filter',
    inventory_plugin = 'Inventory',
    lookup_plugin = 'Lookup',
    netconf_plugin = 'Network Config',
    shell_plugin = 'Shell',
    strategy_plugin = 'Strategy',
    terminal_plugin = 'Terminal',
}

export enum PluginTypes {
    action_plugin = 'Action Plugin',
    cache_plugin = 'Cache Plugin',
    callback_plugin = 'Callback Plugin',
    cliconf_plugin = 'CLI Config Plugin',
    connection_plugin = 'Connection Plugin',
    filter_plugin = 'Filter Plugin',
    inventory_plugin = 'Inventory Plugin',
    lookup_plugin = 'Lookup Plugin',
    netconf_plugin = 'Network Config Plugin',
    shell_plugin = 'Shell Plugin',
    strategy_plugin = 'Strategy Plugin',
    terminal_plugin = 'Terminal Plugin',
}
