- name: Install authorized_keys
  authorized_key:
    state: 'present'
    user: "{{ item | basename | regex_replace('.keys$','') }}"
    key: "{{ lookup('file', item) }}"
  with_fileglob:
    - '*.keys'
  when: authorized_keys is defined
